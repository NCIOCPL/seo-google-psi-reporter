const AbstractGenerator             = require('./abstract-generator');

/**
 * Class represents a URL generator 
 */
class SitemapUrlGenerator extends AbstractUrlGenerator {

    constructor() {
        
    }
}